pipeline {
    agent any

    stages {
        stage('Build docker image') {
            steps {
                sh 'docker build -f jenkins-v2/my-docker-files -t my-node18 .'
            }
        }
    }
}